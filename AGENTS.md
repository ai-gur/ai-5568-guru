# AI 5568 Guru — כללי עבודה

מסמך זה מחייב כל סוכן וכל אדם שנוגע בקוד.

---

## תשעת עקרונות המוצר

אלה **לא ניתנים למשא ומתן**. הם נאכפים בקוד, לא רק בפרוזה.

### 1. לעולם לא לחתום, ולעולם לא "אודיט"

אודיט הוא מעשה של גוף מוסמך. כל שימוש שלנו במילה יוצר בדיוק את המחויבות המשפטית שהעיקרון הזה נועד למנוע.

- הדוח מדווח **ממצאים**. אין בשום מקום "עומד בתקן", "compliant", "מאושר", "תקין לתקן".
- המילה `audit` אסורה בשמות רכיבים, טבלאות, נתיבי API וטקסט משתמש — אלא כשמדובר מפורשות באודיט רשמי של **צד שלישי**.
- חל על ה-UI, ה-API, הסקיל והפלאגין כאחד.

**מונחים מותרים:** סריקה, סקירת מוכנות, ממצא, ליקוי, הנחיית תיקון.
**באנגלית:** review, readiness, scan, finding.

### 2. לא ניתן לאימות = כשל

קריטריון שהבדיקה שלו לא רצה מדווח כ-`FAIL` בביטחון 0, **לעולם לא כ-`PASS`**.

דוח שמדווח "תקין" על משהו שלא הצליח לבדוק הוא מצב הכשל היחיד עם השלכות משפטיות אמיתיות.

### 3. כלל דטרמיניסטי גובר על AI

כשל שנקבע על ידי כלל דטרמיניסטי הוא סמכותי. ה-LLM יכול **להוסיף פירוט** לכשל, ולעולם לא להפוך אותו למעבר.

### 4. NA רק כשהנושא נעדר

- "אין תמונות בעמוד" ⇒ `NA`
- "לכל התמונות יש alt" ⇒ `PASS`

ערבוב בין השניים הוא הדרך הקלאסית שבה דוח נגישות הופך לחסר ערך. כל בדיקת תחולה מפורשת ומחזירה נימוק בעברית.

### 5. אין overlay. אף פעם

לא בפלאגין, לא בסוכן התיקון, ולא כהמלצה במסמך. ווידג'טי overlay מסתירים כשלים במקום לתקן אותם, מגונים בקהילת הנגישות, ונתבעו בבתי משפט.

### 6. אין המצאת חלופות

סוכן שלא יכול לראות תמונה **לא כותב לה טקסט חלופי**. הוא מסמן אותה כדורשת טיפול אנושי.

זה חל גם על תמלול, כתוביות, ותיאורי תרשימים.

### 7. מה שמחייב משפטית מופרד ויזואלית מ-best practice

- **מחייב:** חלק 1 (עמודי אינטרנט), חלק 2 (מסמכים), תוספות ישראליות (`IL`)
- **מומלץ:** מובייל (`MOB`)

קורא הדוח חייב להיות מסוגל לדעת מיד מה נדרש בחוק ומה שיפור מומלץ.

### 8. עדכון תקן לא מתמזג אוטומטית

מכונה מזהה שינוי בתקן ופותחת Pull Request. **בן אדם מאשר.**

שינוי בתקן משפטי שנבדק על ידי מכונה בלבד הוא בדיוק מצב הכשל שהמוצר קיים כדי למנוע.

### 9. כל דוח רושם את גרסת הקטלוג שהפיקה אותו

בלי `catalogueVersion` בדוח, סריקה חוזרת אינה ברת-השוואה ולולאת ה-delta נשברת בשקט. השוואה בין דוחות של גרסאות שונות מציגה מעבר גרסה מפורש ולא מתיימרת להיות delta.

---

## גבולות עבודה

- **אין לשנות** את `C:\AI_Projects\.Skills\נגישות ומשפטי לאתר ישראלי\is5568-auditor` ו-`is-5568-authoring`.
- **אין לשנות** את הפרויקט הישן `C:\AI_Projects\5568 - נגישות`. הוא מקור לקריאה בלבד עד להשלמת ההעברה.
- **שכבת הטופס נכתבת רק על ידי היבואן.** `form.*` בקטלוג מועתק מילולית מטופס הנציבות. אין לערוך אותו ביד, אין לנסח מחדש, ואין "לתקן" מקומות שבהם הטופס סוטה מ-WCAG — הטופס הוא האובייקט המשפטי המחייב.
- **שכבת המנוע נכתבת ביד.** `engine.*` (מיפוי כללים, רובריקות, הוראות תיקון) יושב ב-`overrides.ts` וסביבתו, ולכן ייבוא מחדש של הטופס לא הורס אותו.

## שני ריפואים

| ריפו | תוכן |
|---|---|
| `github.com/ai-gur/ai-5568-guru` — **ציבורי** | `sources/`, `packages/criteria`, `report-contract`, `remediation`, `services/scanner`, `apps/readiness`, `plugins/ai-guru-5568`, `skills/*`, `supabase`, `docs` |
| **פרטי נפרד** | `services/fixer`, `packages/standards-watch`, `licensing/` |

`packages/report-contract` הוא **הגבול בין השניים**. הריפו הפרטי צורך אותו כתלות ואינו מייבא כלום מהציבורי מעבר לו.

## שני ענפים

| ענף | מה יושב שם |
|---|---|
| `development` | **כל הפיתוח.** ברירת המחדל לעבודה ולקומיטים. |
| `main` | **פרודקשן בלבד.** מה שרץ בפועל ב-`readiness.aiguru.co.il`. |

`main` מתעדכן רק כשמשהו נפרס — לא כשמשהו נכתב. הכלל הזה קיים כדי שאפשר יהיה
לענות על "מה רץ עכשיו?" בלי לקרוא היסטוריה: אם זה על `main`, זה באוויר.

**אל תקומיט ישירות ל-`main`.** הרוב המכריע של העבודה בפרויקט הזה קרה על `main`
לפני שהכלל הזה נכתב, ולכן שווה לומר אותו במפורש: קומיט ל-`main` הוא הצהרה
שהקוד נפרס, ואם הוא לא — ההצהרה שקרית.

## אבטחה

- מפתחות API — **צד שרת בלבד**. אף פעם ב-`NEXT_PUBLIC_*` או בכל משתנה שמגיע לדפדפן.
- **אין סודות, כתובות פנימיות או נתוני לקוחות בשום קובץ — גם בריפו הפרטי.**
- חסימת SSRF: טווחים פרטיים, loopback, link-local ו-reserved נחסמים **אחרי פתרון DNS** — לא לפי בדיקת מחרוזת.
- סריקת יעד דורשת הרשאה. סריקה מלאה דורשת הוכחת בעלות על הדומיין.
- כיבוד `robots.txt` ו-rate limiting לכל דומיין יעד.

## סביבה

- פורטים **3000–3999**. אפליקציית ה-Readiness: `3568`. הסורק המקומי: `3569`.
- שמירה על סמנטיקת RTL ושמות נגישים בעברית בכל ממשק עברי.
- הסורק דורש Node.js 22+, Playwright ו-Python. הוא **לא יכול** לרוץ על Cloudflare Workers.

## מוסכמות שמות

| היום (פרויקט ישן) | אחרי |
|---|---|
| `apps/web` | `apps/readiness` |
| `skills/…-accessibility-auditor` | `skills/readiness-client` |
| `audit_jobs` | `review_runs` |
| `audit_projects` | `projects` |
| `audit_artifacts` | `report_artifacts` |
| `POST /api/v1/audits` | `POST /api/v1/reviews` |
| "דוח אודיט" | "דוח סקירת מוכנות" |

הסקיל הישן **לא פורסם** — שינוי השם הוא פעולה פשוטה, ללא מסלול מעבר או הפניה.

מרחב שמות החבילות: `@ai5568/*`.

## פלאגין WordPress

ארכיטקטורת `ai-guru-core`, הכרעת גרסה ורישוי מודע-באנדל **אינן מוגדרות כאן**. הן חוצות-פרויקטים:

> 📄 `C:\AI_Projects\AI Guru WP Platform\docs\plugin-architecture.md`

הפרויקט הזה צרכן של המסמך, לא הבעלים שלו. אין לשכפל את הכללים — יש להפנות אליהם.
