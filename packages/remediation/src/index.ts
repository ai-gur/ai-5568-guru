/**
 * @ai5568/remediation — what has to change, per finding.
 *
 * Public on purpose. The product's line is that the knowledge is free and the
 * labour is paid: anyone can read exactly what to fix and do it by hand. What
 * costs money is having the tool do it.
 */

export { renderRemediation, type RemediationFile } from './render.ts';
